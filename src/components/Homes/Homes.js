import React from 'react';
import './Homes.css';
const Homes = ({review}) => {
    const {name, img, rating, commend} = review;
    return (
        <div>
            
            
        </div>
    );
};

export default Homes;